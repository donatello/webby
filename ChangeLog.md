# Change log

## v1.2.0

- Drop support for GHC < 9.0.
- Test against GHC 9.0 through 9.12.
- Relax upper bounds to next-major for `aeson` (`<3`), `formatting`
  (`<8`), `http-api-data` (`<1`), and `text` (`<3`).

## v1.1.1

- Ensure support for GHC 9.4

## Older releases

Please see commits in the repository.
